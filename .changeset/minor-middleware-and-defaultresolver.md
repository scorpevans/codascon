---
"codascon": minor
"odetovibe": minor
---

Add middleware support (MiddlewareCommand, per-command hooks) and defaultResolver catch-all to codascon; update odetovibe codegen to generate both features.
